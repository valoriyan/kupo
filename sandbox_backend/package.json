{
  "name": "kupo-backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "yarn run killRunningDev && sleep 4 && concurrently \"nodemon\" \"nodemon -x tsoa spec-and-routes\"",
    "deploy": "gcloud builds submit --config 'cloudbuild.yaml' --project valoriyan",
    "build": "tsc",
    "start": "clear && node build/src/server.js"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.4.1",
    "ts-node": "^10.1.0",
    "typescript": "4.5.4"
  },
  "lint-staged": {
    "*.{js,ts}": "eslint --cache --fix",
    "*.{js,ts,css,md}": "prettier --write"
  }
}
