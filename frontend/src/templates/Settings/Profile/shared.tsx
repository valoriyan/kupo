import { Stack } from "#/components/Layout";
import { Body } from "#/components/Typography";
import { styled } from "#/styling";

export const SectionWrapper = styled(Stack, { gap: "$6", p: "$6" });

export const Label = styled(Body, { color: "$secondaryText" });
